import { Cart } from "../../contexts/CartContext";



export function cartReducer(state: Cart, action: any) {
  switch (action) {
    case '':
      break
    default:
      return state;
  }
  return state;
}